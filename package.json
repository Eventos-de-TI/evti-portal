{
  "name": "evti-portal",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --stream",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "deep-freeze": "^0.0.1"
  }
}
